# BACKUP / RECOVERY

V1 不把免费数据库层当作生产级备份 SLA。至少保留定期逻辑导出，并把备份放到站外存储。

建议：
- 初期：每周一次完整逻辑导出。
- 数据量增加：每日一次。
- 发布 migration 前做一次备份。
- 恢复演练至少每季度一次。

恢复顺序：创建数据库 → 执行 migrations → 导入非敏感业务数据 → 验证索引/RLS → 配置环境变量 → Smoke Test。
