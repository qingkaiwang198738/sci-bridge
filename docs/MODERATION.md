# MODERATION / V1.0

## 原则
先发后审，低摩擦发布；通过 Turnstile、写限流、轻量内容风险评分和用户举报降低垃圾/违法内容。

## 风险评分
当前评分是规则型提示分，不等同于法律结论。URL、广告式关键词及明显高风险关键词提高分数。

## 人工审核
`GET /api/v1/admin/reports` 获取 pending 举报；`PATCH` 更新 `actioned` 或 `rejected`。

生产环境应为管理员入口设置独立访问控制，并定期删除/归档不再必要的安全事件。
